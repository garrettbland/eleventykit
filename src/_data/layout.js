/**
 * Define global layout.
 * If you wish to use a different layout in your page,
 * add "layout" key to the front matter and then your new layout name.
 * This is purely here to not have to type in the layout manually on each page
 */

module.exports = 'layout.liquid'
